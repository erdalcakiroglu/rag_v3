"""ragintel — RAG v2 ingestion pipeline (FAZ 1).

Config-first mimari: tüm pipeline parametreleri öncelik zincirinden okunur
(DB `ragintel.app_config` > ENV > kod varsayılanı). Kod içinde sabit parametre
kullanılmaz.
"""

__version__ = "0.1.0"
